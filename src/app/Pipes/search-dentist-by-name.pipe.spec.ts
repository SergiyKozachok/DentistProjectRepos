import { SearchDentistByNamePipe } from './search-dentist-by-name.pipe';

describe('SearchDentistByNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchDentistByNamePipe();
    expect(pipe).toBeTruthy();
  });
});
